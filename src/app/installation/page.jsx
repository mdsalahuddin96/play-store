'use client'


const InstallationPage = () => {
    return (
        <div>
            {/* {installedApp.map(app=><p key={app.id}>{app.name}</p>)} */}
        </div>
    );
};

export default InstallationPage;